datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Loki
        type: loki
        url: http://loki:3100
        # access: proxy  # Optional: You can specify how Grafana accesses the data source
      - name: Prometheus
        type: prometheus
        url: http://prometheus-server:9090
        # access: proxy  # Optional: You can specify how Grafana accesses the data source